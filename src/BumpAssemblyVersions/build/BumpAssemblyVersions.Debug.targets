<?xml version="1.0" encoding="utf-8"?>
<!--For more information on using transformations see the web.config examples at http://go.microsoft.com/fwlink/?LinkId=214134. -->
<Project xmlns:xdt="http://schemas.microsoft.com/XML-Document-Transform">

  <!-- ReSharper disable once UnknownProperty -->
  <Target xdt:Transform="Insert" Name="ReportBumpAssemblyVersionsDebugMessages" AfterTargets="BumpAssemblyVersionsBeforeBuild">
    <!-- ReSharper disable once UnknownProperty -->
    <Warning Text="BumpVersionNewVersion=$(BumpVersionNewVersion)" />
    <!-- ReSharper disable once UnknownProperty -->
    <Warning Text="BumpVersionNewVersion=$(BumpVersionNewAssemblyVersion)" />
    <!-- ReSharper disable once UnknownProperty -->
    <Warning Text="BumpVersionNewVersion=$(BumpVersionNewFileVersion)" />
    <!-- ReSharper disable once UnknownProperty -->
    <Warning Text="BumpVersionNewVersion=$(BumpVersionNewInformationalVersion)" />
    <!-- ReSharper disable once UnknownProperty -->
    <Warning Text="BumpVersionNewVersion=$(BumpVersionNewPackageVersion)" />
  </Target>

</Project>